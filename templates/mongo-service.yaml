apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.database.mongodb.name }}
  namespace: {{ .Values.database.namespace }}
spec:
  ports:
  - port: 27017
    protocol: TCP
    targetPort: 27017
  selector:
    app: {{ .Release.Name }}-mongo
  type: ClusterIP
