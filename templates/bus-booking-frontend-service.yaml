apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.nandhubus.frontend.service.name }}
spec:
  type: {{ .Values.nandhubus.frontend.service.type }}
  ports:
    - protocol: TCP
      port: {{ .Values.nandhubus.frontend.service.port }}
      targetPort: {{ .Values.nandhubus.frontend.service.targetPort }}
  selector:
    app: {{ .Values.nandhubus.backend.name }}