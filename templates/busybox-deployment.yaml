apiVersion: apps/v1
kind: Deployment
metadata:
  name: busybox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: busybox
  template:
    metadata:
      labels:
        app: busybox
    spec:
      containers:
        - name: log-logger
          image: busybox
          command: ['sh', '-c', 'tail -f /var/log/pods/taytelar-ns_backend-65c94fb487-d44lm_3227428a-b606-4e3d-b056-4863224015f0/backend/0.log']
          volumeMounts: 
            - mountPath: /var/log/pods
              name: log-volume
      volumes:
        - name: log-volume
          hostPath:
            path: /var/log/pods/  
            type: DirectoryOrCreate
