kind: PersistentVolume
apiVersion: v1
metadata:
  name: postgres-pv
  labels:
    name: postgres-pv
spec:
  capacity:
    storage: 10Gi
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce
  gcePersistentDisk:
    pdName: postgres-pv
    fsType: ext4
    readOnly: true