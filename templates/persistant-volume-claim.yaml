# pvc-tester.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-tester
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-csi
  resources:
    requests:
      storage: 1Gi