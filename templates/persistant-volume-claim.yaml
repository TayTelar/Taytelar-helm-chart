apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-csi-retain
  ReclaimPolicy : Retain
  resources:
    requests:
      storage: 30Gi